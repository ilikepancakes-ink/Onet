# Server configuration
server:
  name: "Onet File Server"
  version: "1.0.0"
  country: "US"
  port: 3000

# Folders to host
folders:
  - "/home/ilikepancakes/shared"
  - "/home/ilikepancakes/documents"

# Authentication
auth:
  username: "admin"
  password: "password"  # In production, use hashed passwords
  jwt_secret: "your-secret-key-here"
